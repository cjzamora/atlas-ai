# Commerce App Playground

Neutral repo-like fixture for Atlas kernel evals. It models a small commerce SaaS with auth, catalog, checkout, orders, provider webhooks, and ledger sync code.

The files are intentionally compact, but the module layout is realistic enough to exercise retrieval ranking and impacted-test selection without using private project data.
